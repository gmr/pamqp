pamqp.commands
==============
The :py:mod:`pamqp.commands` module is auto-generated, created by the ``tools/codegen.py`` application.

The classes inside :py:mod:`pamqp.commands` allow for the automatic marshaling and unmarshaling of AMQP method frames and `Basic.Properties`. In addition the command classes contain information that designates if they are synchronous commands and if so, what the expected responses are. Each commands arguments are detailed in the class and are listed in the attributes property.

.. automodule:: pamqp.commands
    :members:
